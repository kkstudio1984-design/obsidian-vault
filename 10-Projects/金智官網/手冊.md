# 智廷工程行・金智消防官網｜個人手冊

> 給未來的我自己。6 個月後翻回來找東西用。  
> 最後更新 2026-05-14（正式上線當日凍結點）。

---

## 1. 東西在哪

| 項目 | 位置 |
| --- | --- |
| Code | `D:\GOLD\kingzhi-web`（Next.js 16 App Router + Tailwind + React 19） |
| GitHub | https://github.com/kkstudio1984-design/kingzhi-web（private） |
| Vercel 專案 | `kingzhi-web` 在 `kkstudio1984-designs-projects` team |
| 正式網址 | **https://kingzhi.tw**（2026-05-14 上線、HiNet 註冊、NS 切 Vercel、Let's Encrypt SSL） |
| 備用網址 | https://kingzhi-web.vercel.app（Vercel 預設、永遠跟著正式版） |
| Notion CMS DB | DB ID `34c0fcad-0bc6-8102-8c1d-e42f033d227c`（「SEO 文章管理」） |
| 設計稿原檔 | `D:\GOLD\時尚消防工程 品牌官網-handoff\` |
| SPEC | `D:\GOLD\SPEC.md`（v1.6 + PRD/SDD/TDD） |
| 設計 brief | `D:\GOLD\DESIGN_BRIEF.md` |
| 個人手冊（這份） | `D:\GOLD\kingzhi-handbook.md` + Obsidian `10-Projects/金智官網/手冊.md` |

## 2. 怎麼上一篇 SEO 文章

1. 進 Notion「SEO 文章管理」DB → 新增一列
2. 必填 12 欄位：
   - 文章標題（Title） / Slug（URL 後綴，純英數連字號）
   - 內容類別（📝 部落格／🔧 案例／🛠 服務頁）
   - 關鍵字類別（🔴 商業／🟡 痛點／🟠 檢修申報／🔵 地區）
   - 目標客群（🟢 業主／🏠 管委會）
   - 目標關鍵字 / 字數
   - SEO Title（≤ 60 字）/ SEO Description（≤ 160 字）
   - 封面圖（1200×630 上傳即可，build 時會自動下載到 `public/images/notion/`）
   - 作者（一律 `郭金智｜智廷工程行負責人｜20+ 年消防工程實戰經驗`）
   - 上稿日期
3. 填內文（Notion blocks，支援 H2/H3、清單、引用、圖、表）
4. 「狀態」改成 **已上稿**（草稿狀態不會出公網）
5. 按頁面上方 **🚀 立刻發布** 按鈕 → 打 webhook → ISR 重建
6. 5–10 秒後 `https://kingzhi.tw/knowledge/<slug>` 上線
7. 想暫存：「狀態」維持「草稿」即可

> ⚠️ **不要把作者欄位寫成「消防設備師證照加持」** — 客戶定位是消防工程廠商，不是設備師（SPEC AC-18 紅線）

## 3. 常見維護

| 場景 | 動作 |
| --- | --- |
| 改文章內容/錯字 | Notion 編輯 → 🚀 立刻發布 |
| 改首頁 / Footer / Hero | 改 code → `git push main` → Vercel 自動部署 ~40s |
| 加 / 換底圖 | 丟 `public/images/*.jpg` → push |
| 看詢問通知 | 王小姐 Gmail `a0925369737@gmail.com`（手機 push 接近即時） |
| 改聯絡電話/地址 | 主要在 `components/Footer.tsx` 與 `lib/schema.ts` |
| 改 metadata title/og | `app/layout.tsx` |

## 4. 環境變數（Vercel Production）

| Key | 用途 |
| --- | --- |
| `NOTION_TOKEN` | Notion integration secret，讀 SEO DB |
| `NOTION_DATABASE_ID` | SEO 文章管理 DB ID |
| `REVALIDATE_SECRET` | Notion 🚀 按鈕 webhook header 驗證 |
| `RESEND_API_KEY` | 寄信（**金智專屬新 Resend 帳號**的 key，跟個人站老帳號分開） |
| `CONTACT_EMAIL_TO` | 詢問表單收件信箱（`a0925369737@gmail.com` 王小姐） |
| `LINE_CHANNEL_ACCESS_TOKEN` | LINE Messaging API（**業主決定不做**，可不設） |
| `LINE_TARGET_GROUP_ID` | 同上、不設 |
| `NEXT_PUBLIC_SITE_URL` | `https://kingzhi.tw` |
| `NEXT_PUBLIC_GA_ID` | GA4（待設） |

**本機 dev 拉 env：**

```bash
cd D:\GOLD\kingzhi-web
npx vercel env pull .env.local --environment=production
```

⚠️ Vercel 把所有 secret 變數都標 **Sensitive**，pull 下來會是 `""` 空字串。要本機跑真實 Notion 串接的話，得從 Notion integration 設定頁手動拷貝 token 進 `.env.local`。否則就走 `lib/mock-data.ts` 的 8 篇 fallback。

## 5. 部署流程

```bash
git push origin main
```

→ GitHub → Vercel 自動偵測 → 約 40 秒上線。

**Git author 限制：** 這個 repo 的 commit 必須用 `kkstudio1984@gmail.com` 當 author email（Vercel Hobby + private repo 限制；全域設定的其他 email 會被擋）。本 repo 已 local config 過了，新機器要 `git config user.email kkstudio1984@gmail.com`。

**回滾：** Vercel dashboard → 專案 → Deployments → 舊版本 → ⋯ → Promote to Production。

## 6. 看成效

| 工具 | 看什麼 |
| --- | --- |
| Vercel Analytics | 流量、Web Vitals（dashboard 自帶） |
| GA4 | 事件 `phone_click`, `line_click`, `contact_submit`（埋在 `lib/analytics.ts`、待 `NEXT_PUBLIC_GA_ID` 設定才會送） |
| Search Console | SEO 排名、impression、click（Domain 屬性 `kingzhi.tw` 已驗證、sitemap 已提交） |
| Resend dashboard | 寄信成功率、bounce（新帳號工作區） |

## 7. 出事 SOP

| 症狀 | 先看哪 |
| --- | --- |
| 部署失敗 | Vercel → Deployments → 點失敗那筆 → Build Logs |
| Notion 改完沒更新 | 1) 確認狀態=已上稿 2) 重按 🚀 3) Vercel → Functions logs 看 `/api/revalidate` |
| 文章圖全壞 | 多半是 Notion S3 簽名過期，build pipeline 沒下載到。重 deploy 觸發 `lib/image-pipeline.ts` |
| Resend 寄不出去 | RESEND_API_KEY 無效、域名 SPF/DKIM 沒驗、月免費額度爆 |
| LINE 沒推播 | bot 被踢出群、LINE_TARGET_GROUP_ID 改了、token 過期 |

## 8. 待辦（2026-05-14 上線後狀態）

### 還在的
- ⏸️ **30 篇 SEO 文章** 漸進補滿（目前 Notion 上 14 篇）
- ⏸️ **GA4 NEXT_PUBLIC_GA_ID** 設定（業主要先開 GA4 屬性、取 Measurement ID）
- ⏸️ **OG 圖再升級**：目前 `app/opengraph-image.tsx` 動態產文字版、底圖純漸層；想加原 AI 黑金光點底圖可以加 `<img>` 元素（要絕對 URL）

### 業主決定不做（已從待辦移除）
- 🚫 **LINE Messaging API** — Gmail push 已接近即時，業主表示不需要再接 LINE
- 🚫 **TWNIC 域名安全鎖** — NT$4,800/年付費服務、CP 值不合；改靠 60 天轉出鎖（自動）+ HiNet 帳號 2FA

### 已完成（2026-05-14 收）
- ✅ **kingzhi.tw 上線** — HiNet 註冊（年費 NT$800，到期 2027-05-14、轉出鎖到 2026-07-13）、NS 切 `ns1/ns2.vercel-dns.com`、Let's Encrypt SSL 自動發
- ✅ **HiNet 帳號雙因子認證** 啟用
- ✅ **Resend `kingzhi.tw` 驗證**（DKIM/SPF/DMARC，region Tokyo `ap-northeast-1`，用 Resend Auto configure + Vercel 整合一鍵設妥）
- ✅ **Resend 帳號分流**：金智專屬新帳號（API key `kingzhi-web-prod`），個人站 `kkstudio1984.com` 走老帳號；老帳號 `kingzhi-web-test` key 已 revoke
- ✅ **Contact form `from`** 改 `noreply@kingzhi.tw`，端對端到王小姐 `a0925369737@gmail.com` `delivered`
- ✅ **GSC Domain 屬性驗證**（DNS TXT）+ sitemap.xml 提交
- ✅ **Next.js 14.2.35 → 16.2.6 + React 18 → 19.2.6** 安全升級。code 改動：
  - `components/icons.tsx`：global `JSX.Element` → `React.JSX.Element`（React 19 移除 global JSX namespace）
  - `tsconfig.json`：`jsx: preserve` → `react-jsx`（Next 16 預設）、include 加 `.next/dev/types`
  - 3 個 `[slug]/page.tsx`：`params: { slug }` → `params: Promise<{ slug }>`（Next 15+ breaking）— hotfix PR #16，否則動態頁全 404
- ✅ **OG 圖**：og-default.jpg 從 1.3MB PNG 偽 `.jpg` 重壓 55KB 真 JPEG 1200×630；首頁改用 `app/opengraph-image.tsx` 動態產品牌字圖（Noto Sans TC subset、黑金 luxury）
- ✅ **Hero / og-default / 作者頭像** 三張底圖都已進 `public/images/`

## 9. 重要決策（為什麼這樣選）

| 決策 | 理由 |
| --- | --- |
| 主品牌：智廷工程行（金智消防為副） | 2026-05-07 老闆堅持以登記名稱為主；金智消防保留並陳承接 SEO |
| 視覺：黑金 luxury（非原 SPEC 的深藍+橘） | Claude Design 出稿後採用，B2B 信任感更強 |
| 年資寫法：「負責人 20+ 年實戰」 | 公司 2023 設立，個人 20+ 年 — 公平交易法切開避免虛偽表示 |
| 不放 Google Map | 地址是住商混合 3 樓住家，隱私考量（Q11 已決定） |
| 不寫「消防設備師」字樣 | 客戶定位是消防工程廠商而非設備師（AC-18 紅線） |
| 30 篇 SEO 文章 | 撐起「基隆消防」「消防安檢」「消防工程」長尾流量 |
| Notion 為 CMS（非自建後台） | 王小姐/老闆熟 Notion 介面，零學習成本 |
| Vercel Hobby 方案 | 流量小、足夠用；超量再升 |

## 10. 人

| 角色 | 聯繫方式 |
| --- | --- |
| 我（開發者） | kkstudio1984@gmail.com |
| 王小姐（對外接電話） | 0925-369-737 / `a0925369737@gmail.com` |
| 郭金智（老闆） | 同上電話（唯一一支） |
| 公司登記 | 智廷工程行，統編 92528634，基隆市安樂區基金一路 135 巷 40 號 3 樓，2023-03-16 設立 |

## 11. 常用指令速查

```bash
# 啟本機 dev
cd D:\GOLD\kingzhi-web
npm run dev                         # http://localhost:3000

# 拉 Vercel env（sensitive 會是空字串）
npx vercel env pull .env.local --environment=production

# 部署
git add -A
git commit -m "feat: ..."
git push origin main

# 看 Vercel 部署狀態
npx vercel ls
npx vercel logs <deployment-url>

# Notion 重整一篇文章（不靠 🚀 按鈕）
curl -X POST https://kingzhi.tw/api/revalidate \
  -H "x-revalidate-secret: <REVALIDATE_SECRET>" \
  -d '{"slug":"<slug>","route":"knowledge"}'
```
